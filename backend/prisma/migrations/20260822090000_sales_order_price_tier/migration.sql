-- PHASE 8B — เก็บระดับราคาที่ใช้ตั้งต้นราคาของออเดอร์
--
-- additive อย่างเดียว: เพิ่มคอลัมน์ nullable ตัวเดียว ไม่แตะข้อมูลเดิม
-- ออเดอร์ที่มีอยู่แล้วจะได้ค่า NULL = "ไม่ได้ระบุระดับราคา" ซึ่งเป็นความจริง
-- ห้าม backfill ย้อนหลัง เพราะเดาไม่ได้ว่าตอนนั้นผู้ใช้เลือกระดับใด
-- (ราคาที่ใช้จริงอาจถูก override จนไม่ตรงกับระดับใดเลย)
--
-- ความจริงทางการเงินยังอยู่ที่ sales_order_items.unitPrice / lineTotal เหมือนเดิม
ALTER TABLE `sales_orders` ADD COLUMN `priceTier` VARCHAR(191) NULL;
