import { useEffect, useState, type FormEvent } from 'react';
import { AlertTriangle, Boxes, CheckCircle2, ClipboardCheck, FileDown, PackageCheck, Plus, Search, Trash2, Truck, Warehouse } from 'lucide-react';
import { Link, useLocation, useNavigate } from 'react-router-dom';
import { apiClient } from '@/lib/api-client';
import { useToast } from '@/components/ui/Toast';

type WarehouseOption = { id: string; code: string; name: string };
type Supplier = { id: string; code: string; name: string };
type Stock = { warehouseId: string; onHand: string; reserved: string };
type Item = { id: string; code: string; name: string; type: string; imageUrl?: string; lastCost: string; purchaseToBaseFactor: string; baseUnit: { code: string; name: string }; purchaseUnit?: { code: string; name: string }; stockBalances: Stock[] };
type Order = { id: string; orderNo: string; deliveryDate: string; deliveryTime?: string; status: string; customer: { name: string }; items: { menuNameSnapshot: string; quantity: string }[] };
type Lookups = { warehouses: WarehouseOption[]; suppliers: Supplier[]; items: Item[]; orders: Order[] };
type Receipt = { id: string; receiptNo: string; receiptDate: string; status: string; createdById?: string; supplier?: { name: string }; warehouse: { name: string }; items: { id: string; quantity: string; unitPrice: string; totalCost: string; lotNo?: string; expiryDate?: string; item: { name: string; code: string } }[] };
type Issue = { id: string; issueNo: string; issueDate: string; issuedAt?: string; status: string; order?: { orderNo: string; customer: { name: string } }; createdBy: { fullName: string }; items: { id: string; issuedQty: string; unit: string }[] };
type ReceiptLine = { key: string; itemId: string; quantity: number; unitPrice: number; lotNo: string; expiryDate: string };
type IssueLine = { key: string; itemId: string; name: string; requiredQty: number; issuedQty: number; unit: string };

const money = (value: number | string) => `฿${Number(value).toLocaleString('th-TH', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
const today = () => new Date().toISOString().slice(0, 10);
const freshReceiptLine = (): ReceiptLine => ({ key: crypto.randomUUID(), itemId: '', quantity: 1, unitPrice: 0, lotNo: '', expiryDate: '' });
const openDocument = async (path:string) => { const blob=await apiClient.blob(path); const url=URL.createObjectURL(blob); window.open(url,'_blank','noopener,noreferrer'); window.setTimeout(()=>URL.revokeObjectURL(url),60000); };

function WorkspaceHero({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: React.ReactNode }) {
  return <header className="ops-hero"><div><span>{eyebrow}</span><h1>{title}</h1><p>{description}</p></div>{action}</header>;
}

function Kpis({ items }: { items: { label: string; value: string | number; icon: typeof Boxes; tone?: string }[] }) {
  return <div className="ops-kpis">{items.map(({ label, value, icon: Icon, tone }) => <article key={label} className={tone ?? ''}><Icon/><div><span>{label}</span><strong>{value}</strong></div></article>)}</div>;
}

export function ReceivingPage() {
  const {toast}=useToast();
  const navigate = useNavigate(); const { pathname } = useLocation(); const creating = pathname.endsWith('/new');
  const [receipts, setReceipts] = useState<Receipt[]>([]); const [lookups, setLookups] = useState<Lookups>(); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const [query, setQuery] = useState('');
  const [lines, setLines] = useState<ReceiptLine[]>([freshReceiptLine()]);
  const load = async () => { try { const [history, options] = await Promise.all([apiClient.get<Receipt[]>('/business/receiving'), apiClient.get<Lookups>('/business/operations/lookups')]); setReceipts(history); setLookups(options); } catch (reason) { setError(reason instanceof Error ? reason.message : 'โหลดข้อมูลงานรับของไม่สำเร็จ'); } };
  useEffect(() => { void load(); }, []);
  const total = lines.reduce((sum, line) => sum + line.quantity * line.unitPrice, 0); const filtered = receipts.filter((r) => `${r.receiptNo} ${r.supplier?.name ?? ''}`.toLowerCase().includes(query.toLowerCase()));
  const updateLine = (key: string, field: keyof ReceiptLine, value: string | number) => setLines((current) => current.map((line) => line.key === key ? { ...line, [field]: value } : line));
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (busy) return; setBusy(true); setError(''); const data = new FormData(event.currentTarget); try { await apiClient.post('/business/receiving', { receiptNo: data.get('receiptNo'), warehouseId: data.get('warehouseId'), supplierId: data.get('supplierId') || undefined, receiptDate: data.get('receiptDate'), note: data.get('note') || undefined, items: lines.map(({ itemId, quantity, unitPrice, lotNo, expiryDate }) => ({ itemId, quantity, unitPrice, lotNo: lotNo || undefined, expiryDate: expiryDate || undefined })) }); await load(); toast({title:'รับของเข้าสำเร็จ',description:'Stock เพิ่มและ Price History ถูกบันทึกเรียบร้อยแล้ว',variant:'success'}); navigate('/receiving'); } catch (reason) { const message=reason instanceof Error ? reason.message : 'ยืนยันการรับของไม่สำเร็จ';setError(message);toast({title:'รับของเข้าไม่สำเร็จ',description:message,variant:'error'}); } finally { setBusy(false); } };
  if (creating) return <section className="ops-page"><WorkspaceHero eyebrow="GOODS RECEIPT WORKSPACE" title="รับวัตถุดิบ / บรรจุภัณฑ์เข้า" description="บันทึกการรับ เพิ่ม Stock และเก็บประวัติราคาซื้อในธุรกรรมเดียว" action={<Link className="ops-secondary" to="/receiving">กลับหน้ารายการ</Link>}/>{error && <div className="auth-alert">{error}</div>}<form className="ops-form-layout" onSubmit={(e) => void submit(e)}><main>
    <section className="ops-section"><div className="ops-section-title"><Truck/><div><h2>ข้อมูลการรับ</h2><p>ระบุเอกสาร คลัง และคู่ค้าของรายการรับครั้งนี้</p></div></div><div className="ops-fields"><label>เลขที่รับ<input name="receiptNo" defaultValue={`GR-${Date.now().toString().slice(-8)}`} required/></label><label>วันที่รับ<input name="receiptDate" type="date" defaultValue={today()} required/></label><label>คลัง<select name="warehouseId" required><option value="">เลือกคลัง</option>{lookups?.warehouses.map((w) => <option key={w.id} value={w.id}>{w.code} · {w.name}</option>)}</select></label><label>Supplier<select name="supplierId"><option value="">ไม่ระบุ</option>{lookups?.suppliers.map((s) => <option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="wide">หมายเหตุ<textarea name="note" placeholder="รายละเอียดการรับหรือเลขเอกสารอ้างอิง"/></label></div></section>
    <section className="ops-section"><div className="ops-section-title"><Boxes/><div><h2>รายการรับเข้า</h2><p>ค่าปริมาณรวมและต้นทุนคำนวณจากข้อมูลจริงในแต่ละแถว</p></div><button type="button" onClick={() => setLines((v) => [...v, freshReceiptLine()])}><Plus/> เพิ่มรายการ</button></div><div className="ops-line-table receipt-lines"><div className="ops-line-head"><span>รายการ</span><span>จำนวน</span><span>ราคาซื้อ</span><span>ระบบคำนวณ</span><span>Lot / หมดอายุ</span><span/></div>{lines.map((line) => { const item = lookups?.items.find((entry) => entry.id === line.itemId); return <div className="ops-line" key={line.key}><select value={line.itemId} onChange={(e) => { const selected = lookups?.items.find((i) => i.id === e.target.value); updateLine(line.key, 'itemId', e.target.value); if (selected && !line.unitPrice) updateLine(line.key, 'unitPrice', Number(selected.lastCost)); }} required><option value="">เลือกวัตถุดิบ / บรรจุภัณฑ์</option>{lookups?.items.map((i) => <option key={i.id} value={i.id}>{i.code} · {i.name}</option>)}</select><label><input type="number" min="0.0001" step="0.0001" value={line.quantity} onChange={(e) => updateLine(line.key, 'quantity', Number(e.target.value))}/><small>{item?.purchaseUnit?.code ?? item?.baseUnit.code ?? 'หน่วย'}</small></label><input type="number" min="0" step="0.01" value={line.unitPrice} onChange={(e) => updateLine(line.key, 'unitPrice', Number(e.target.value))}/><div className="calculated"><small>ระบบคำนวณ</small><strong>{money(line.quantity * line.unitPrice)}</strong><span>{item ? `${line.quantity * Number(item.purchaseToBaseFactor)} ${item.baseUnit.code}` : '—'}</span></div><div className="lot-fields"><input placeholder="Lot" value={line.lotNo} onChange={(e) => updateLine(line.key, 'lotNo', e.target.value)}/><input type="date" value={line.expiryDate} onChange={(e) => updateLine(line.key, 'expiryDate', e.target.value)}/></div><button type="button" className="remove-line" disabled={lines.length === 1} onClick={() => setLines((v) => v.filter((x) => x.key !== line.key))}><Trash2/></button></div>; })}</div></section>
  </main><aside className="ops-summary"><small>RECEIPT SUMMARY</small><h2>สรุปการรับ</h2><dl><div><dt>จำนวนรายการ</dt><dd>{lines.filter((l) => l.itemId).length}</dd></div><div><dt>มี Lot</dt><dd>{lines.filter((l) => l.lotNo).length}</dd></div><div><dt>มีวันหมดอายุ</dt><dd>{lines.filter((l) => l.expiryDate).length}</dd></div><div className="total"><dt>มูลค่ารับรวม</dt><dd>{money(total)}</dd></div></dl><div className="ops-confirm-note"><CheckCircle2/><span>เมื่อยืนยัน Stock จะเพิ่มและ Price History จะถูกบันทึก</span></div><button className="ops-primary" disabled={busy || !lines.every((l) => l.itemId)}><PackageCheck/>{busy ? 'กำลังบันทึก…' : 'ยืนยันรับของ'}</button></aside></form></section>;
  const receivedToday = receipts.filter((r) => r.receiptDate.slice(0,10) === today()); const valueToday = receivedToday.flatMap((r) => r.items).reduce((s,i) => s + Number(i.totalCost), 0);
  return <section className="ops-page"><WorkspaceHero eyebrow="INBOUND OPERATIONS" title="รับของเข้า" description="ติดตามวัตถุดิบและบรรจุภัณฑ์ที่เพิ่มเข้าคลัง พร้อมประวัติราคาและ Lot" action={<Link className="ops-primary link" to="/receiving/new"><Plus/> รับของเข้า</Link>}/><Kpis items={[{label:'รับวันนี้',value:receivedToday.length,icon:Truck},{label:'จำนวนรายการวันนี้',value:receivedToday.flatMap((r)=>r.items).length,icon:Boxes},{label:'มูลค่ารับวันนี้',value:money(valueToday),icon:PackageCheck},{label:'เอกสารทั้งหมด',value:receipts.length,icon:ClipboardCheck}]}/><div className="ops-toolbar"><Search/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="ค้นหาเลขที่รับหรือ Supplier"/></div>{error&&<div className="auth-alert">{error}</div>}<div className="ops-history-table"><div className="ops-history-head"><span>เลขที่รับ</span><span>วันที่</span><span>Supplier / คลัง</span><span>รายการ</span><span>มูลค่ารวม</span><span>เอกสาร</span></div>{filtered.map((receipt)=><article key={receipt.id}><strong>{receipt.receiptNo}</strong><span>{new Date(receipt.receiptDate).toLocaleDateString('th-TH')}</span><span>{receipt.supplier?.name ?? 'ไม่ระบุ'}<small>{receipt.warehouse.name}</small></span><span>{receipt.items.length}</span><span className="money">{money(receipt.items.reduce((s,i)=>s+Number(i.totalCost),0))}</span><button className="doc-action" onClick={()=>void openDocument(`/business/documents/GOODS_RECEIPT_SLIP/${receipt.id}.pdf`)}><FileDown/> PDF</button></article>)}</div>{!filtered.length&&<div className="business-empty"><Truck/><h2>ยังไม่มีรายการรับของ</h2><p>เริ่มรับของเพื่อเพิ่ม Stock และบันทึกราคาซื้อจริง</p><Link to="/receiving/new">+ รับของเข้า</Link></div>}</section>;
}

export function StockIssuePage() {
  const {toast}=useToast();
  const navigate = useNavigate(); const { pathname } = useLocation(); const creating = pathname.endsWith('/new'); const [lookups,setLookups]=useState<Lookups>(); const [issues,setIssues]=useState<Issue[]>([]); const [warehouseId,setWarehouseId]=useState(''); const [orderId,setOrderId]=useState(''); const [lines,setLines]=useState<IssueLine[]>([]); const [busy,setBusy]=useState(false); const [error,setError]=useState('');
  const load=async()=>{try{const [options,history]=await Promise.all([apiClient.get<Lookups>('/business/operations/lookups'),apiClient.get<Issue[]>('/business/stock-issues')]);setLookups(options);setIssues(history);}catch(reason){setError(reason instanceof Error?reason.message:'โหลดข้อมูลงานเบิกไม่สำเร็จ');}}; useEffect(()=>{void load();},[]);
  const loadDemand=async(id:string)=>{setOrderId(id);if(!id){setLines([]);return;}try{const demand=await apiClient.get<{items:{itemId:string;name:string;unit:string;quantity:string}[];warnings:string[]}>(`/business/orders/${id}/demand`);setLines(demand.items.map((item)=>({key:crypto.randomUUID(),itemId:item.itemId,name:item.name,requiredQty:Number(item.quantity),issuedQty:Number(item.quantity),unit:item.unit})));setError(demand.warnings.join(' · '));}catch(reason){setError(reason instanceof Error?reason.message:'คำนวณความต้องการไม่สำเร็จ');}};
  const available=(itemId:string)=>lookups?.items.find((i)=>i.id===itemId)?.stockBalances.filter((b)=>!warehouseId||b.warehouseId===warehouseId).reduce((s,b)=>s+Number(b.onHand)-Number(b.reserved),0)??0; const insufficient=lines.filter((l)=>l.issuedQty>available(l.itemId));
  const submit=async()=>{if(busy||!warehouseId||!lines.length||insufficient.length)return;if(!window.confirm('ยืนยันการเบิก? ระบบจะตัด Stock ทันที'))return;setBusy(true);try{await apiClient.post('/business/stock-issues',{issueNo:`SI-${Date.now().toString().slice(-8)}`,warehouseId,orderId:orderId||undefined,idempotencyKey:crypto.randomUUID(),items:lines.map((l)=>({itemId:l.itemId,requiredQty:l.requiredQty,issuedQty:l.issuedQty,baseQty:l.issuedQty,unit:l.unit}))});await load();toast({title:'เบิกของให้ครัวกลางแล้ว',description:'Stock ถูกตัดเรียบร้อยและไม่สามารถกดซ้ำได้',variant:'success'});navigate('/stock-issues');}catch(reason){const message=reason instanceof Error?reason.message:'ยืนยันการเบิกไม่สำเร็จ';setError(message);toast({title:'เบิกของไม่สำเร็จ',description:message,variant:'error'});}finally{setBusy(false);}};
  if(creating)return <section className="ops-page"><WorkspaceHero eyebrow="CENTRAL KITCHEN ISSUE" title="เบิกของให้ครัวกลาง" description="เตรียมวัตถุดิบและบรรจุภัณฑ์ตามออเดอร์ พร้อมตัด Stock ทันทีเมื่อยืนยัน" action={<Link className="ops-secondary" to="/stock-issues">กลับหน้ารายการ</Link>}/>{error&&<div className="auth-alert">{error}</div>}<div className="ops-form-layout"><main><section className="ops-section"><div className="ops-section-title"><ClipboardCheck/><div><h2>เลือกออเดอร์และคลัง</h2><p>ระบบจะโหลดความต้องการจากสูตรที่ใช้งานอยู่โดยอัตโนมัติ</p></div></div><div className="ops-fields"><label>Order<select value={orderId} onChange={(e)=>void loadDemand(e.target.value)}><option value="">เลือก Order</option>{lookups?.orders.map((o)=><option key={o.id} value={o.id}>{o.orderNo} · {o.customer.name} · {new Date(o.deliveryDate).toLocaleDateString('th-TH')}</option>)}</select></label><label>คลังที่ตัด<select value={warehouseId} onChange={(e)=>setWarehouseId(e.target.value)} required><option value="">เลือกคลัง</option>{lookups?.warehouses.map((w)=><option key={w.id} value={w.id}>{w.code} · {w.name}</option>)}</select></label></div>{orderId&&<div className="order-preview">{lookups?.orders.find((o)=>o.id===orderId)?.items.map((i)=><span key={i.menuNameSnapshot}>{i.menuNameSnapshot} × {Number(i.quantity)}</span>)}</div>}</section><section className="ops-section"><div className="ops-section-title"><Warehouse/><div><h2>รายการที่จะเบิก</h2><p>ตรวจสอบคงเหลือและจำนวนหลังเบิกก่อนยืนยัน</p></div></div><div className="issue-grid"><div className="issue-head"><span>รายการ</span><span>ต้องใช้</span><span>คงเหลือ</span><span>เบิก</span><span>หลังเบิก</span><span>ความพร้อม</span></div>{lines.map((line)=>{const stock=available(line.itemId);const after=stock-line.issuedQty;const state=after<0?'short':after<=line.issuedQty*.2?'low':'ready';return <div className="issue-row" key={line.key}><strong>{line.name}<small>{line.unit}</small></strong><span>{line.requiredQty.toLocaleString()}</span><span>{stock.toLocaleString()}</span><input type="number" min="0.0001" step="0.0001" value={line.issuedQty} onChange={(e)=>setLines((v)=>v.map((x)=>x.key===line.key?{...x,issuedQty:Number(e.target.value)}:x))}/><span>{after.toLocaleString()}</span><span className={`stock-state ${state}`}>{state==='ready'?'พร้อม':state==='low'?'ใกล้หมด':'ไม่พอ'}</span></div>})}</div>{!lines.length&&<div className="business-empty"><Boxes/><h2>เลือก Order เพื่อโหลดความต้องการ</h2><p>ระบบใช้สูตรจริงของแต่ละเมนูในการรวมวัตถุดิบ</p></div>}</section></main><aside className="ops-summary"><small>ISSUE READINESS</small><h2>สรุปการเบิก</h2><dl><div><dt>จำนวนรายการ</dt><dd>{lines.length}</dd></div><div><dt>พร้อมเบิก</dt><dd>{lines.length-insufficient.length}</dd></div><div><dt>ของไม่พอ</dt><dd className={insufficient.length?'danger':''}>{insufficient.length}</dd></div><div className="total"><dt>จำนวนที่จะตัด</dt><dd>{lines.reduce((s,l)=>s+l.issuedQty,0).toLocaleString()}</dd></div></dl><div className="ops-warning"><AlertTriangle/><span>เมื่อกด “ยืนยันการเบิก” ระบบจะตัด Stock ทันที</span></div><button className="ops-primary" disabled={busy||!warehouseId||!lines.length||Boolean(insufficient.length)} onClick={()=>void submit()}><PackageCheck/>{busy?'กำลังตัด Stock…':'ยืนยันการเบิก'}</button></aside></div></section>;
  return <section className="ops-page"><WorkspaceHero eyebrow="OUTBOUND OPERATIONS" title="เบิกของให้ครัวกลาง" description="ประวัติการตัด Stock ตามออเดอร์และงานเตรียมของครัวกลาง" action={<Link className="ops-primary link" to="/stock-issues/new"><Plus/> สร้างใบเบิก</Link>}/><Kpis items={[{label:'ใบเบิกทั้งหมด',value:issues.length,icon:ClipboardCheck},{label:'เบิกวันนี้',value:issues.filter((i)=>(i.issuedAt??i.issueDate).slice(0,10)===today()).length,icon:PackageCheck},{label:'รายการที่ตัดแล้ว',value:issues.flatMap((i)=>i.items).length,icon:Boxes},{label:'รอดำเนินการ',value:issues.filter((i)=>i.status==='DRAFT').length,icon:AlertTriangle}]}/><div className="ops-history-table"><div className="ops-history-head issue-history"><span>เลขที่เบิก</span><span>วันที่</span><span>Order / ลูกค้า</span><span>รายการ</span><span>ผู้เบิก</span><span>เอกสาร</span></div>{issues.map((issue)=><article className="issue-history" key={issue.id}><strong>{issue.issueNo}</strong><span>{new Date(issue.issuedAt??issue.issueDate).toLocaleDateString('th-TH')}</span><span>{issue.order?.orderNo??'ไม่ผูก Order'}<small>{issue.order?.customer.name}</small></span><span>{issue.items.length}</span><span>{issue.createdBy.fullName}</span><button className="doc-action" onClick={()=>void openDocument(`/business/documents/STOCK_ISSUE_SLIP/${issue.id}.pdf`)}><FileDown/> PDF</button></article>)}</div>{!issues.length&&<div className="business-empty"><Warehouse/><h2>ยังไม่มีรายการเบิกของ</h2><Link to="/stock-issues/new">+ สร้างใบเบิก</Link></div>}</section>;
}
